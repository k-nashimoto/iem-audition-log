# ROADMAP.md — 機能拡張ロードマップ

段階を踏んで拡張する。各フェーズは前フェーズが安定してから着手。
（背景・好みプロファイル・現状構成は `CLAUDE.md` 参照）

---

## フェーズ1：今の単一HTMLの強化（すぐ着手可・低リスク）

単一ファイル＋localStorage のまま実現できる範囲。

- [x] **機種横断の比較ビュー**: 複数 session を選び、8カテゴリ×評価(◎○△✕)をマトリクスで一覧。（2026-06-29）
- [x] **メーカー別 傾向分析**: メーカー単位で全試聴を集計し、カテゴリ別平均・◎数を比較。各メーカーの「得意／注意カテゴリ」を自動抽出して表示。比較ビューに「機種別／メーカー別」モード切替を追加。（2026-06-29）
- [~] **可視化**: カテゴリ別のバーで機種/メーカー比較（◎=4…✕=1 のスコア化、平均バー＋シンボル）。レーダーチャートは未着手。外部ライブラリは使わず素のCSS/HTMLで（オフライン維持）。
- [x] **サマリー**: session ごとに「◎の数・平均スコア」を自動集計（合計行）。メーカー別は得意/注意カテゴリも。（2026-06-29）
- [ ] **エクスポート整備**: JSON に加え CSV 書出（表計算で比較したい用途）。
- [ ] **検索/並べ替え**: 一覧を機種名・日付・スコアでソート/フィルタ。
- [ ] **品質**: localStorage 満杯/破損時のフォールバック、リセットの粒度（全消去/session単位）。

完了条件: 「Kronos vs VE10 vs 他機種」を1画面で比較できる。→ 機種別・メーカー別の両軸で達成。

---

## フェーズ2：IEM情報の検索・蓄積（要・設計判断）

「世の中のIEM情報」をどう持つかが論点。自動収集は規約・安定性の問題があるため、**自分で育てるローカルDBから**。

- [ ] **IEMマスタのデータモデル設計**: `{ id, brand, model, price, driverConfig, signature, notes, links[], owned, status }` 等。
- [ ] **手入力＋メモ**でIEM情報を蓄積（このスレッドで作った Noble/VE/Forte Ears 等の情報を初期データに）。
- [ ] session と IEMマスタを紐付け（機種名フリーテキスト → マスタ参照へ）。
- [ ] 参考リンク集（レビュー/中古相場/メーカー）をIEMごとに保持。
- [ ] （任意・半自動）検索補助: 公式ブログ/レビューサイトのURLを貼ると要点をメモ欄に整形、等。フルスクレイピングは規約・安定性から避け、人手レビュー前提で。
- [ ] **アーキテクチャ判断**: ここで単一HTML→「静的サイト（複数ファイル/簡易フレームワーク）＋localStorageまたはIndexedDB」への移行を検討。必要なら軽量バックエンド（データ共有・端末横断同期が欲しくなった場合）。

決定が必要: ローカル完結を続けるか、クラウド同期を入れるか。→ まずローカル完結で。

---

## フェーズ3：Qobuz / Apple Music 連携（要・API調査・最も重い）

外部API・認証が絡む。実現性調査から。

- [ ] **Apple Music**: MusicKit JS の調査（Apple Developer 登録・デベロッパートークン・ユーザートークンが必要）。プレイリスト読込→チェックリスト自動展開、Web上での再生連携が現実的なゴール。
- [ ] **Qobuz**: 公式公開APIが限定的。実現可能な範囲（メタデータ取得/プレイリスト連携の可否）をまず調査。難しければ「手動同期＋リンク」に留める。
- [ ] **プレイリスト→チェックリスト自動マッピング**: 連携で取得した曲を、好みプロファイルに基づき8カテゴリへ半自動割当（最終は人手調整）。
- [ ] 認証情報・トークンの安全な扱い（クライアントのみで完結しない場合はバックエンド必須＝フェーズ2のアーキ判断と連動）。

注意: ここはオフライン前提と相反する。連携機能は任意・オンライン時のみ動く形にし、コア（試聴ログ）はオフラインで完結させ続ける。

---

## 横断課題

- [ ] データのバックアップ/端末間同期（JSON手動 → 将来クラウド）。
- [ ] 規模が増えたらフレームワーク（Vite + 軽量フレームワーク等）への移行を検討。ただしオフライン/デプロイの手軽さは維持。
- [ ] 簡単なテスト（評価ロジック・集計・データ移行）。

---

## 決定ログ（Decision Log）

新しい設計判断はここに追記していく。

- 2026-06: コアは単一HTML＋localStorage＋GitHub Pages を維持（オフライン・ホーム画面起動が要件のため）。
- 2026-06: 評価軸は8カテゴリ(00–07)、評価記号は ◎○△✕。曲は Apple Music「試聴用プレイリスト」(22曲)準拠、`CATS` 配列を source of truth とする。
- 2026-06-29: フェーズ1「機種横断の比較ビュー」を実装。比較対象は固定機種を持たず、ユーザーが任意の session を選択する方式（VE10/Kronos のハードコードはしない）。スコアリングは ◎=4 / ○=3 / △=2 / ✕=1、未評価は集計から除外。各セルは平均バー＋平均シンボル＋◎数·評価済/全曲を表示。
- 2026-06-29: 既存 localStorage キー `audition-log-v1` を温存し、`store` に `compareIds: string[]` を追加する非破壊スキーマ拡張で対応。session 削除時は `compareIds` からも除去、未知 ID は描画時に自動クリーンアップ。
- 2026-06-29: session に `maker`（メーカー名）を機種名と別の独立フィールドとして追加。表記揺れ防止のため主要メーカーを datalist 候補化。メーカー単位の集計・傾向分析の基盤とする（機種名フリーテキストへ混在させない）。`maker` は任意・後方互換。
- 2026-07-02: **単一ファイル制約を撤廃し複数ファイル構成へ移行**（案A：ビルドなしESモジュール）。`iem-audition-log.html`（シェル）＋`styles/app.css`＋`src/{data,core,ui,main}.js` の layered 構成（data←core←ui、循環依存なし）。JSは `<script type="module">`。PWA化（`sw.js`＋`manifest.webmanifest`＋`icon.svg`、network-first＋オフラインフォールバック）でホーム画面オフライン動作を明示的に担保。オフライン維持・外部依存なし・push即反映は不変、`localStorage`/データ構造も不変。制約：ESモジュールは `http(s)` 前提のため `file://` 直開き不可（ローカルは http サーバで確認）。Vite等のビルド導入はフェーズ2/3で規模が増えた段階に先送り。ui.js のビュー別さらなる分割は今後の余地。
- 2026-07-02: **カタログ（曲リスト）と評価データの分離設計・レベル1**を実装。`CATS` を `CATEGORIES`（カテゴリ定義）＋ `TRACKS`（曲リスト＝source of truth、`cat` で所属参照・安定id）に正規化し、`CATS=CATEGORIES.map(...tracks: TRACKS.filter)` で描画用ビューを合成（既存の描画/集計はビューを使うため無改修）。曲リスト改訂は `TRACKS` の編集だけで完結。`CATALOG_VERSION`（現"2.2"）を導入し、新規 session に採点時の `catalogVersion` を記録。`CATALOG_IDS` で**孤児評価**（現行カタログに無い曲の評価）を判定し、集計（progress/goldCount/sessStat/aggSessStat）からは除外しつつデータは保持・書出にも含める。詳細画面にカタログ版と孤児件数を注記表示。保存先はコード内定数のまま（オフライン単一ファイル維持）。アプリ内編集（レベル2）は必要時に着手。
- 2026-07-02: 試聴リスト**v2.2**へ更新＋カテゴリ定義を拡張。方針＝メインカテゴリ00–07は不変（既存記録と互換）。(1) 各 track に**安定ID**（slug）を付与し、rating/notes/openMemo のキーを位置ベース(`01-0`)から安定IDへ移行（`OLD_ID_MAP` による冪等マイグレーション、除外曲プロローグ/丸ノ内サディスティックの評価は破棄、序曲のマーチは番外(EX)へ）。(2) サブ観点を track の**タグ**として実装（01a/01b, 04a/04b/04c, 05a/05b, 06a/06b/06c）。スコアは曲単位のまま＝サブ観点評価＝曲評価。カテゴリ評価は従来どおり曲評価の平均（別枠の手動メインスコアは持たない）。(3) 04 は「◎は3タイプすべて○以上」ヒントと、サブ観点の現在評価サマリー(04a/04b/04c)をUI表示。(4) §4の注目ポイントを各曲の `a` に反映、★店頭コア7を `core` フラグで表示。(5) 「★店頭コア7のみ」表示トグルを追加（一次スクリーニング用）。試聴条件メモ（サービス/DAP設定/アッテネーター）は既存の再生アプリ・ソース・接続/コーデック/ケーブル欄で代替。打ち切り記録(§5.2)は優先度低のため未実装。番外(EX)は愛着枠として詳細で記録可（比較にも行として出るが評価軸ではない）。
- 2026-06-29: 比較ビューに「機種別／メーカー別」モードを追加。メーカー別は全 session をメーカーで grouping し、カテゴリ平均・◎数を集計、得意（最高avg）/注意（最低avg）カテゴリを抽出表示。スコア化は機種別と共通（◎=4…✕=1、未評価は集計から除外）。状態は `store.cmpMode`（'session'|'maker'）と `store.compareMakers: string[]` で保持（非破壊拡張、`compareMakers` 未定義＝初回は全メーカー選択）。メーカー未設定の session は「(メーカー未設定)」として1グループに集約。グリッド組立は機種別・メーカー別で `buildGrid()` を共用。
- 2026-07-02: **DESIGN.md（Spotify着想）に沿って全体のビジュアルを刷新**。デザイントークンを DESIGN.md 基準へ差し替え（`--bg #121212`/`--card #181818`/hover `#282828`/bar `#000`/border `#2a2a2a`/text 白・`#b3b3b3`・`#6a6a6a`、単一グリーン `--green #1db954`）。主要CTA=green塗り黒文字pill、戻る/セカンダリ=透過＋`#535353`枠白文字pill、進捗/アクティブ/選択=green、フォーカス=greenリング。**方針：単一グリーン原則の厳密適用ではなく「機能色は保持」を採用**（オーナー選択）＝評価記号 ◎○△✕・優先度タグ・サブ観点タグ・カテゴリ左帯/番号・比較ビューのシンボル/バーは従来の金/鋼/シアン/警告色を維持（判別性優先）。装飾的な steel/cyan（メーカーラベル・接続チップ・STEP0タグ等）はグレースケール化。フォントは Circular/Montserrat を外部読込せず system-ui+日本語システムフォントで代替（オフライン維持）。テーマカラー/アイコン/manifest も新ブランドへ更新、SWキャッシュを v2 に。CLAUDE.md §6 を DESIGN.md 参照＋適用方針に改訂。
- （以後追記）

## 未決事項（Open Questions）

- クラウド同期は必要か？（複数端末で使うか）
- IEM情報はどこまで自動化するか（手動DB / 半自動 / 自動）。
- フェーズ3の連携はApple Music優先か、両対応か。
